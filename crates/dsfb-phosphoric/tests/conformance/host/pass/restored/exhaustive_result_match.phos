module tests.conformance.host.pass.restored.exhaustive_result_match;
profile host;

// pass-expected
// (restored from legacy_ui/pass/exhaustive_result_match)

module demo;
fn choose(value: Result[u32, u32]) -> u64 {
    match value {
        Ok(inner) => 1,
        Err(code) => 0,
    }
}
