module tests.conformance.host.pass.restored.compatible_if_expression;
profile host;

// pass-expected
// (restored from legacy_ui/pass/compatible_if_expression)

module pass.compatible_if_expression;

fn choose(flag: bool, left: u32, right: u32) -> u32 {
    if flag { left } else { right }
}
