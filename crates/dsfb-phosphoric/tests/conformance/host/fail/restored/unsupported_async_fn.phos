module tests.conformance.host.fail.restored.unsupported_async_fn;
profile host;

// fail-expected: P-003
// (restored from legacy_ui/fail/unsupported_async_fn; legacy code UNSUPPORTED_SYNTAX "unexpected token at item start")

module fail.unsupported_async_fn;

async fn bad() {
    return;
}
