module tests.conformance.host.fail.restored.unsupported_trait;
profile host;

// fail-expected: P-003
// (restored from legacy_ui/fail/unsupported_trait; legacy code UNSUPPORTED_SYNTAX "unexpected token at item start")

module fail.unsupported_trait;

trait Draw;
