module tests.conformance.host.fail.restored.non_exhaustive_result_match;
profile host;

// fail-expected: K-016
// (restored from legacy_ui/fail/non_exhaustive_result_match; legacy code NON_EXHAUSTIVE_MATCH "missing branches: Err")

module demo;
fn choose(value: Result[u32, u32]) -> u64 {
    match value {
        Ok(inner) => 1,
    }
}
