module fixture.residual_r1_byte_layout;
profile boot;

struct Residual {
    kind: u8,
    arch_id: u8,
    seq: u16,
    cycle: u64,
    payload: [u8; 14],
    chain_hash: [u8; 4],
}

fn entry() -> i32 {
    return 0;
}
