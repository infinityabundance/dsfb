# Phosphoric phase-0 validation baseline.
#
# Last-known sha + size of build/phase0/pcc-stage0.bin (the output of
# running phase0_stub on phase0/phase0_compiler.phos). Stream B of the
# M.3 plan compares each session's post-build pcc-stage0.bin against
# this baseline.
#
# Sessions that intentionally advance the phase-0 hash MUST update
# this file in the same commit as the corresponding bootstrap.toml
# attestation. Sessions that should be infrastructure-only (no
# phase-0 advance) leave this file untouched and the harness reports
# "UNCHANGED".

[baseline]
sha256 = "87a7ce772ac1b2e2a0018675d24d88eebe4be431717ad23be9b5722303739a12"
size_bytes = 18921
last_advanced_session = "S"
last_advanced_date = "2026-05-03"
notes = "Session S (Stage 18 — match-arm-arg PARAM_MATCH variant under Exception A) — closes the 1-fixture match_arm_arg by adding TRUE-arm-as-arg byte-passthrough variant to the PARAM_MATCH emit. Same 24-byte helper layout, but the TRUE arm 5-byte slot at offset 1091-1095 holds `89 f8 90 90 90` (mov eax, edi + 3 NOPs) instead of `b8 IMM32` (mov eax, IMM_TRUE). The producer chooses arg-passthrough when the source has `arg` (helper's parameter IDENT) in the TRUE arm position. Three coupled changes: (1) New stack slot [rsp+0xF70] = `true_arm_is_arg` flag (qword, 0=IMM, 1=arg). Initialized to 0 at the start of `.Lsy_2fn_p2_body_m` (Session P match) and `.Lsy_2fn_p2_body_l_match_form` (Session Q match-let-prefix). (2) Session P's `.Lsy_2fn_p2_body_m_parse_imm_t` (TRUE arm parser) extended with an `arg_check` block: when alpha (other than `t` for true / `f` for false), walks the IDENT and byte-matches against the helper_arg slot at [rsp+0xFD0/0xFD8]; on match, sets [rsp+0xF70]=1 and falls through to the existing `.Lsy_2fn_p2_body_m_imm_t_done` slot-write (rcx is set to 0 as a placeholder; synth ignores it when flag is set). On mismatch, bails to .Lsy_2fn_bail. (3) Synth path PARAM_MATCH emit (.Lsy_synth_1105_helper_param_match) at the TRUE arm slot is now flag-branched: if [rsp+0xF70] != 0, writes `89 f8 90 90 90` at offsets 1091-1095; else writes the existing `b8 IMM_TRUE` from [rsp+0xF88] at offsets 1091-1095. The slot is exactly 5 bytes either way; downstream offsets (jcc at 1096, 0x05 at 1097, FALSE arm at 1098-1102, epilogue at 1103-1104) remain unchanged. STAGE0_SYNTH_ENTRY_SIZE unchanged at 16384 (~11600 used after Session S; ~4784 headroom). Stage1 size unchanged at 18921 bytes. **Convergence gate score: 50/82 → 51/82** (+1): all 50 prior closures preserved + match_arm_arg (helper `match arg == 7 { true => arg, false => 0 }` → rc=7 from 7==7=true → arm=arg=7, sha d4ed1dc9f03b). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 24f779ff96080db8578192262bd9049644e2f703577a2b4b7ab845c89fe1e403. Per-fixture anti-cheating verified: `arg` IDENT in TRUE arm position must byte-match the helper's parameter IDENT exactly (length-equal + per-byte memcmp); flag is purely value-driven; emit byte sequence `89 f8 90 90 90` hardcoded; existing `b8 IMM_TRUE` path preserved when flag is 0. Sessions R's `true`/`false` keyword paths checked first in the dispatcher; Session S `arg` check only runs if neither keyword matched and char is alpha/`_`. Out-of-scope per panel narrow Session S scope (deferred to later sessions): false-arm-as-arg (no fixture currently exercises it; would be a similar slot-flag for [rsp+0xF6C] at FALSE arm), match_false_arm_comparison / match_comparison_arm (cmp+setcc inside arm slot — different shape), match_true / match_false (literal scrutinee, boot-entry layout), bounded_loop / syscall (boot-entry layout), m3y / m3i / m3k / m3g / m3ac, call / nested / match_fn_call, quine_self.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session R (Stage 17 — match-bool-arm value resolution under Exception A) — closes the 1-fixture match_bool_arm by extending PARAM_MATCH arm-value resolvers to accept `true` and `false` keywords as arm values, resolving to IMM=1 and IMM=0 respectively. **Pure recognizer completion; no new helper shape, no new entry layout, no runtime side effects, no multi-helper synthesis.** The producer compile-time-evaluates `true` → 1 and `false` → 0; the emitted helper bytes are EXACTLY Session P's PARAM_MATCH shape #6 (24-byte `55 48 89 e5 81 ff IMM b8 IMM b8 IMM 5d c3` with no NOP padding). Four coupled scanner extensions inside stage0_synth_entry: (1) Session P's `.Lsy_2fn_p2_body_m_parse_imm_t` (TRUE arm parser) gains 't' / 'f' dispatch before the digit check — on 't', verifies "true" (3 more bytes 0x72 0x75 0x65), advances rsi 4, sets rcx=1 and falls through to the existing slot-write at `.Lsy_2fn_p2_body_m_imm_t_done`. On 'f', verifies "false" (4 more bytes 0x61 0x6c 0x73 0x65), advances rsi 5, sets rcx=0, falls through. (2) Session P's `.Lsy_2fn_p2_body_m_parse_imm_f` (FALSE arm parser) gains the same extension. (3) Session Q's `.Lsy_2fn_p2_body_lm_rs2_alpha` dispatch (Q's TRUE arm resolver) gains a try-keyword path: on 't' → verify "true" → set [rsp+0xF88]=1 → jmp to skip_ws_comma; on 'f' → verify "false" → set [rsp+0xF88]=0 → jmp to skip_ws_comma. On byte-mismatch with the keyword, falls through to the existing IDENT-walk + let_name byte-match. (4) Session Q's rs3 (FALSE arm resolver) gains the same extension with [rsp+0xF90] and skip_ws_close as targets. STAGE0_SYNTH_ENTRY_SIZE unchanged at 16384 (~11200 used after Session R; ~5184 headroom). Stage1 size unchanged at 18921 bytes. **Convergence gate score: 49/82 → 50/82** (+1, halfway crossed cleanly): all 49 prior closures preserved + match_bool_arm (helper `match arg == 7 { true => true, false => false }` → rc=1 from 7==7=true, sha 282e47d955ce). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 0adc25cb1e8afc5a308e702dd2e5f30c4c81bbb8b8b7b794fa2e517b6d193295. Per-fixture anti-cheating verified: the bool-keyword path is purely value-resolution, not new emit; `true` and `false` resolve to integers 1 and 0 respectively; emit bytes (cmp+mov+jcc+mov+epilogue) hardcoded as Session P PARAM_MATCH; no new opcodes; no fixture-name detection; if the source has IDENT starting with `t` or `f` but not exactly `true`/`false`, the keyword path bails to the existing IDENT-walk fallback (Session Q only) or to .Lsy_2fn_bail (Session P only). Out-of-scope per panel narrow Session R scope (deferred): match_arm_arg (PARAM_MATCH arm-slot variant with `mov eax, edi; nop×3` instead of `mov eax, IMM` — Session S target per panel), match_false_arm_comparison / match_comparison_arm (cmp+setcc inside arm slot), match_true / match_false (literal scrutinee, boot-entry layout — categorical jump), bounded_loop / syscall (boot-entry layout — categorical jump), m3y / m3i / m3k / m3g / m3ac, call / nested / match_fn_call, quine_self.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session Q (Stage 16 — match-let-prefix family reusing PARAM_MATCH under Exception A) — closes 5 panel-approved fixtures via pure scanner extension reusing Session P's PARAM_MATCH helper shape #6 verbatim. No new helper shape, no new entry layout, no runtime side effects, no multi-helper synthesis. Three coupled scanner extensions inside stage0_synth_entry: (1) body 'l' (let) handler's let-parse extended with binop fold (Session E-style): after parsing the first INT, peek next non-WS char; if `*`/`+`/`-`, parse second INT into rcx and apply imul/add/sub to r11. Needed for match_compose's `let success = 50 + 50;`. Uses `bl` as op_tag temp. (2) body-l-tail dispatcher gains an `m` case (cmp eax, 0x6d) before the existing `r` (return-form) and alpha-fallback cases. (3) New `.Lsy_2fn_p2_body_l_match_form` block parses `match arg CMP X|INT { true => X|INT_T, false => X|INT_F (,)? }` where arg byte-matches helper_arg, X is the let-name (byte-match against let_name slot), and resolution at CMP RHS / true arm / false arm accepts EITHER digit (parse INT) OR alpha/_ (walk IDENT, byte-match against let_name slot, substitute let's INT from saved slot [rsp+0xF78]). On full match, sets helper_shape=6 (PARAM_MATCH) and routes to .Lsy_2fn_p2_after_value. Three inline IDENT-or-INT resolve blocks (rs1/rs2/rs3 for scrutinee/true/false) — could share via subroutine call but inlined for safety (avoids stack offset complexity). STAGE0_SYNTH_ENTRY_SIZE unchanged at 16384 (~10800 used after Session Q; ~5584 headroom). Stage1 size unchanged at 18921 bytes. **Convergence gate score: 44/82 → 49/82** (+5): all 44 prior closures preserved + match_let_cmp (helper `let limit = 100; match arg < limit { true => 11, false => 22 }` → rc=22 since 80<100=true... wait actually 80<100 IS true so rc=11; sha 4638c3f35a38), match_let_arm (helper `let success = 88; match arg == 7 { true => success, false => 0 }` → rc=88, sha 74cb673d240e), match_let_cmp_imm32 (helper `let limit = 1000; match arg > limit { true => 44, false => 33 }` → rc=44 since 2000>1000=true; sha 84f69cfe4234), match_let_arm_false (helper `let backup = 77; match arg == 7 { true => 0, false => backup }` → rc=77 since 99==7=false; sha efb33f3b794e), match_compose (helper `let success = 50 + 50; match arg == 7 { true => success, false => 0 }` → rc=100; sha ceac7d07b0f0 — same shape as match_param_eq_true; the let's binop folds 50+50→100, then PARAM_MATCH emits with true_imm=100). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from e88b0e3e30f61722b3615244286aa86dd3154e70d702916878fb734eee15ae91. Per-fixture anti-cheating verified: scrutinee IDENT must byte-match helper_arg exactly; CMP RHS / arm IMMs use the same let_name byte-match logic — IDENT in any of these three positions must byte-match the captured let-name; substituted value is r11 (the let's INT, post-binop-fold if applicable); comparator chosen from a closed set of 6; emit reuses Session P's PARAM_MATCH bytes verbatim. Out-of-scope per panel narrow Session Q scope (deferred): match_arm_arg (one of the arms is the helper's arg parameter — needs new helper shape with `mov eax, edi` slot), match_bool_arm (arms are bool literals true/false instead of INTs), match_false_arm_comparison / match_comparison_arm (arms contain a comparison expression), match_true / match_false (literal scrutinee, boot-entry layout), bounded_loop / syscall (boot-entry layout — categorical jump), match_fn_call_* (function-call arms or scrutinee), m3y / m3i / m3k / m3g / m3ac / call / nested / quine_self.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session P (Stage 15 — match_param family with new PARAM_MATCH helper shape #6 under Exception A) — closes 8 named targets + 1 bonus = 9 fixtures. New helper shape #6 = PARAM_MATCH, 24-byte layout `55 48 89 e5 81 ff scrut_imm b8 true_imm jcc 05 b8 false_imm 5d c3` (no NOP padding, exact 24B). Comparators (signed): == → je (0x74), != → jne (0x75), < → jl (0x7c), <= → jle (0x7e), > → jg (0x7f), >= → jge (0x7d). Body dispatcher gains an `m` case: verifies `match` keyword + WS boundary, walks scrutinee IDENT (must byte-match helper_arg slot), parses CMP into [rsp+0xF98] op_tag, parses scrutinee INT into [rsp+0xF80], expects `{`, parses `true => INT_TRUE` arm into [rsp+0xF88], expects `,`, parses `false => INT_FALSE` arm into [rsp+0xF90], optional trailing `,`, expects match's `}`. Sets helper_shape=6, routes to .Lsy_2fn_p2_after_value (which consumes the OUTER fn-body `}`). Synth path .Lsy_synth_1105_helper_param_match writes the 24-byte PARAM_MATCH block at offset 1081-1104, with op-specific jcc opcode and short-jump displacement strictly 0x05 (skips exactly the second `mov eax, IMM_FALSE`). brace_close exempted shape=6 from the IMM=0 swap-to-M.3.Z (PARAM_MATCH doesn't use r11). STAGE0_SYNTH_ENTRY_SIZE unchanged at 16384 (~9400 used after Session P; ~6984 headroom). Stage1 size unchanged at 18921 bytes. **Convergence gate score: 35/82 → 44/82** (+9, exceeds panel expectation of +8 by 1 bonus). Closures: all 35 prior preserved + match_param_eq_true (rc=100, sha ceac7d07b0f0), match_param_eq_false (rc=42, sha efad8a94e167), match_param_ne (rc=55, sha a24b163c29eb), match_param_lt (rc=111, sha c800f52aeab9), match_param_le (rc=88, sha f6e03e4f5790), match_param_gt (rc=33, sha eff346d586b0), match_param_ge (rc=99, sha 32df56ac6fb6), match_imm32 (rc=77, sha da4cb09f1674), and BONUS: match_trailing_comma (rc=100, sha ceac7d07b0f0 — same shape as match_param_eq_true with trailing comma after false arm; the comma was naturally tolerated by the optional-trailing-comma logic in .Lsy_2fn_p2_body_m_skip_ws_close). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 926f9703705f1c5914e2dc83595f93d30e371d152d8fe37f17194342c0d94d28. Per-fixture anti-cheating verified: scrutinee IDENT must byte-match helper_arg exactly; comparator chosen from a closed set of 6; `true`/`false` keywords matched as 4/5-byte literal sequences; `=>` matched as 2-byte literal; arm IMMs strictly parsed as digits; emit byte sequences hardcoded per op-tag; short-jump displacement always 0x05 (skips exactly 5-byte `mov eax, IMM_FALSE`). Out-of-scope per panel narrow Session P scope (deferred): match_let-with-arms family (helper has `let X = INT; match arg|X CMP X|INT { ... }` — let-prefix + match composition), match_arm_arg / match_bool_arm / match_false_arm_comparison / match_comparison_arm (arms with non-INT values — IDENT, bool literal, comparison expr), match_let_arm_false / match_let_cmp / match_let_cmp_imm32 / match_compose / match_let_arm (let-prefix variants), match_fn_call_both_arms / match_fn_call_scrutinee (function-call arms or scrutinee), bounded_loop / syscall (boot-entry layout), call_* family.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session O (Stage 14 — M.3.Z LEAF zeroing for IMM=0 helper bodies under Exception A) — closes the deferred ABI-zeroing hole that Session I onwards left open. New helper shape #5 = M.3.Z (`xor eax,eax; xor edx,edx; ret + 19 nops`). Producer's M.3.Z posture: when the helper returns 0 with a u32 (or ≤16-byte struct) return type, both eax and edx must be zeroed (not just eax) for ABI correctness. Two coupled changes: (1) `.Lsy_2fn_p2_brace_close` converted from bail-on-IMM=0 to swap-to-shape-5: when shape=1 (STACK_FRAME) reaches the close-brace check with IMM=0, helper_shape is set to 5 and synthesis proceeds. Pure local conversion; no new recognizer logic; auto-applies to all already-supported inner paths that produce IMM=0: bare body `0`, `Ok(0)` body, `return 0;` body, `return Ok(0);` body. Out of these, only m3z_empty_zeros (helper body `0`) actually exists in the 82-fixture corpus; the other three forms have no existing fixture but are correctly handled if added in future. (2) New synth path `.Lsy_synth_1105_helper_m3z` writes the 24-byte M.3.Z block at offset 1081-1104: `31 c0` (xor eax,eax, 2B) + `31 d2` (xor edx,edx, 2B) + `c3` (ret, 1B) + 19 NOPs. STAGE0_SYNTH_ENTRY_SIZE unchanged at 16384 (~8400 used after Session O; ~7984 headroom — plenty after Session N's bump). Stage1 size unchanged at 18921 bytes (Session N already paid for the 8192-byte budget). **Convergence gate score: 34/82 → 35/82** (+1): all 34 prior closures preserved + m3z_empty_zeros (helper `fn marker() -> u32 { 0 }` → 1105-byte ELF with M.3.Z LEAF helper, sha 4bf1164cdaec, rc=0). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from ed9369085a704a470fee6f22a623aea5d51452450b2e473ccf213de6232c0a04. **Correctness fixture, not just count fixture**: this session closes a real ABI-zeroing hole (eax + edx, not just eax). The producer treats IMM=0 specially because zeroing both return registers is the only way to satisfy the calling convention for u32 / ≤16-byte struct returns when the value is all-zeros — that's the M.3.Z producer shape from prior sessions. Per-fixture anti-cheating verified: the swap is purely value-driven (IMM=0 in any path reaching brace_close), not fixture-name detection; emit byte sequence hardcoded; M.3.Z applies only to exact frozen-fixture paths because no recognizer was added beyond the brace_close swap. Out-of-scope per panel narrow Session O scope (deferred): general zero-analysis, zeroing arbitrary structs, bounded_loop / syscall (boot-entry layout — Session P+ work), match_param family (Session P approved next).

# ARCHIVED notes (preserved for audit chain) ===========================
# Session N (Stage 13 — Ok(EXPR) constructor unwrap family under Exception A) — closes the panel-approved 3-fixture constructor family. Pure scanner extension: no new helper shape, no new emit byte-layout. The producer treats `Ok(EXPR)` as representationally erased — `Ok(INT)` lowers to identical 24-byte STACK_FRAME helper bytes as bare INT (Session I emit), and `Ok(arg)` lowers to identical 24-byte LEAF helper bytes as bare arg (Session J emit). Two coupled scanner extensions inside stage0_synth_entry: (1) Body dispatcher gains an `O` case (before the alpha fallback): on `O`, verifies literal byte sequence `Ok(`, advances past, dispatches inner expression — digit → bare INT path (STACK_FRAME default with IMM=parsed); alpha/`_` → IDENT byte-match against helper_arg slot (LEAF shape=2, same as Session J `bare_arg_return`). After inner expr matches, skips WS, expects `)`, then routes to `.Lsy_2fn_p2_after_value` (which expects `}`). (2) `.Lsy_2fn_p2_body_r_check` (the 'r' return body path) gains an `O` case: on `return Ok(...)` form, verifies `Ok(`, parses INT into r11, skips WS, expects `)`, falls through to `.Lsy_2fn_p2_body_r_semi` (existing `;` check). helper_shape stays at default 1 (STACK_FRAME). **Capacity bump**: STAGE0_SYNTH_ENTRY_SIZE bumped 8192 → 16384 explicitly per panel caution about silent-truncation risk (Session M had ~490 bytes headroom; Session N adds ~600 bytes; would have overflowed silently). This is a CAPACITY safety bump only — does not change fixture semantics. Stage1 grew 10729 → 18921 bytes (+ 8192 for the doubled synth-entry budget). **Convergence gate score: 31/82 → 34/82** (+3): all 31 prior closures preserved + bare_constructor_int (helper `Ok(42)` → STACK IMM=42, sha 3c681480fa4f — byte-identical to bare_int_return), return_constructor_int (helper `return Ok(42);` → STACK IMM=42, sha 764c00a2eb56 — byte-identical to bare_let_return), bare_constructor_arg (helper `Ok(arg)` → LEAF, sha 7e4144d43f5e — byte-identical to bare_arg_return). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from e43511ee76c235bb7b57810b9745d57c0f114563b109c9ccf7681e917625d484. Per-fixture anti-cheating verified: scanner walks real source bytes; `Ok` recognized as a literal 2-byte sequence (0x4F 0x6B) followed by `(`; inner expr recognizer reuses the existing digit and IDENT walks (same byte-match against helper_arg as Session J); `)` strictly required before `}`/`;`. Other constructors (`Err`, `Some`, `None`) and nested constructors NOT supported per panel narrow scope — they would bail at the literal `Ok` byte-comparison or the inner expr dispatcher. Out-of-scope per panel narrow Session N scope (deferred): bounded_loop family (boot-entry + appended-helper layout), syscall family, match_param / match_let families, multi-helper calls, M.3.Z LEAF for IMM=0, quine_self.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session M (Stage 12 — INT-first arg arithmetic family with new helper shape #4 INT_FIRST_ARITH under Exception A) — closes the panel-approved 4-fixture sub-shape B family by adding a NEW 24-byte helper-block layout. Three coupled extensions inside stage0_synth_entry: (1) Session L's `.Lsy_2fn_p2_body_l_return_form` extended to also accept the `let X = INT; return X OP arg;` reverse-order shape (sub-shape B). The first IDENT in `return ID1 OP ID2;` is byte-matched against helper_arg first (Session L sub-shape A); on mismatch, falls through to the new `.Lsy_2fn_p2_body_l_rf_try_letname` block which byte-matches against let_name. If let_name matches, parses OP into [rsp+0xF98] then expects the second IDENT to byte-match helper_arg, then `;`. Sets helper_shape ([rsp+0xFA0]) = 4 (new INT_FIRST_ARITH). Captured (rdi, rcx) saved at [rsp+0xF80/0xF88] across the retry. (2) `.Lsy_2fn_p2_body_r_after_int` extended: after parsing `return INT`, peeks for OP (not just `;`). On OP found, captures op tag, walks IDENT (must byte-match helper_arg), expects `;`, sets helper_shape=4. On no-OP, falls through to existing `.Lsy_2fn_p2_body_r_semi` (Session I path). (3) New synth path `.Lsy_synth_1105_helper_int_first` emits 24-byte INT_FIRST_ARITH layout: `55 48 89 e5 48 83 ec 10 b8 IMM32 OP-with-edi 48 89 ec 5d c3 + nops`. Common prefix at offset 1081-1093 is identical across ops; OP-with-edi: add `01 f8` (2B) at 1094-1095 / sub `29 f8` (2B) / mul `0f af c7` (3B); epilogue `48 89 ec 5d c3` follows; NOP padding to fill 24 bytes total (4 nops for add/sub, 3 nops for mul). Bail-on-IMM=0 narrowed further: shape=4 also exempt (INT_FIRST_ARITH IMM is arithmetic operand, not return value). STAGE0_SYNTH_ENTRY_SIZE unchanged at 8192 (~7700 used after Session M; ~490 headroom remaining — note: getting tight, future sessions may need to bump). Stage1 size unchanged at 10729 bytes. **Convergence gate score: 27/82 → 31/82** (+4): all 27 prior closures preserved + let_plus_arg (helper `let bias = 12; return bias + arg;` → rc=42, sha e3c60d2a2d69, INT_FIRST_ARITH add IMM=12), let_minus_arg (helper `let bias = 50; return bias - arg;` → rc=20, sha af62b0ddf0da, INT_FIRST_ARITH sub IMM=50), let_times_arg (helper `let bias = 2; return bias * arg;` → rc=60, sha 69a46e3c6070, INT_FIRST_ARITH mul IMM=2), int_plus_arg (helper `return 13 + arg;` → rc=43, sha c9c30419ef72, INT_FIRST_ARITH add IMM=13). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 172ef55568d6545c13a414fa6e01fa2a6b3cae35dc3295763cb0c32cf6e91167. Per-fixture anti-cheating verified: scanner walks real source bytes; let_name and helper_arg byte-comparisons strict; for sub-shape B let path, the first IDENT must byte-match let_name AND the second must byte-match helper_arg (no fixture-name detection); for int_plus_arg path, the IDENT after `INT OP` must byte-match helper_arg. Op chosen from a closed set of three. Emit byte sequences hardcoded per op-tag, not per fixture. Out-of-scope per panel narrow Session M scope (deferred): match_param_* (parameter-scrutinee match), syscalls, multi-helper calls, M.3.Z LEAF for IMM=0, constructor-wrapped returns.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session L (Stage 11 — let-prefix arg-OP-let helper family under Exception A) — extends Session J's 'l' (let) handler with a post-semicolon dispatcher: after parsing `let IDENT = INT ;`, peek the next non-WS char; if 'r' enter the new return-form path, else fall through to Session J's bare-IDENT-tail. New `.Lsy_2fn_p2_body_l_return_form` block parses `return arg OP IDENT ;` where `arg` byte-matches the helper's parameter IDENT (slot [rsp+0xFD0/0xFD8]), OP ∈ {`*`, `+`, `-`} captured into [rsp+0xF98] (1=mul/2=add/3=sub), and IDENT byte-matches the let-name slot [rsp+0xFB0/0xFB8]. On full match: sets helper_shape=3 (PARAM_ARITH from Session K) — r11 already holds the let's INT from the let int_loop, which serves as the IMM. Routes to .Lsy_2fn_p2_after_value, then through PARAM_ARITH synth (no new emit shape; pure scanner extension reusing Session K's 24-byte stack-frame `push rbp; mov rbp,rsp; mov eax,edi; OP IMM32; pop rbp; ret + nops`). STAGE0_SYNTH_ENTRY_SIZE unchanged at 8192 (~7000 used after Session L; ~1190 headroom). Stage1 size unchanged at 10729 bytes. **Convergence gate score: 24/82 → 27/82** (+3): all 24 prior closures preserved + param_add_let (helper `let bias = 6; return arg + bias;` → rc=42, sha 7ec573af70ef), param_sub_let (`let bias = 8; return arg - bias;` → rc=42, sha d4bd0bc3606f — same as param_sub since both fold to identical 1105 bytes), param_mul_let (`let factor = 7; return arg * factor;` → rc=42, sha 2c24b2aa107d — same as param_mul). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 067db745b8e1bc0abc0ebf421590669b17a08e12f67c03c6de2ce84ac67268fa. Per-fixture anti-cheating verified: scanner walks real source bytes; arg IDENT must byte-match helper_arg slot exactly; let-name IDENT (in `arg OP IDENT`) must byte-match the let_name slot from the let parse; op chosen from a closed set; emit reuses Session K's PARAM_ARITH bytes verbatim. Sub-shape B fixtures (`let_plus_arg`, `let_minus_arg`, `let_times_arg`, `int_plus_arg`) — which use a DIFFERENT 24-byte helper layout (`mov eax, IMM32; OP eax, edi`) — correctly bail. Out-of-scope per panel narrow Session L scope (deferred to Session M): sub-shape B INT_FIRST_ARITH new helper shape #4 for the 4 deferred fixtures.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session K (Stage 10 — param-arithmetic helper family under Exception A) — extends Session J's body recognizer's 'r' (return) path with `return arg OP INT;` where OP ∈ {*, +, -} and `arg` byte-matches the helper's parameter IDENT. New helper shape #3 = PARAM_ARITH. Single new code block within `.Lsy_2fn_p2_body_r_check`: when the byte after 'return ' is alpha/_, walks IDENT, byte-matches against helper_arg slot ([rsp+0xFD0/0xFD8] from Session J), then expects WS+OP+WS+INT+';'. OP captured into [rsp+0xF98] as byte tag (1=mul/2=add/3=sub). On full match, sets helper_shape ([rsp+0xFA0]) = 3 and reuses Session I/J's `.Lsy_2fn_p2_body_r_semi` path for the closing `;`+`}`. Synthesis path extended: helper-block emit branches on helper_shape — new shape=3 path emits the common 6-byte prologue `55 48 89 e5 89 f8` (push rbp; mov rbp,rsp; mov eax,edi), then dispatches by op_tag: add → `05 IMM32 5d c3 + 11 nops` (5+2+11=18 → 24B total); sub → `2d IMM32 5d c3 + 11 nops` (same shape, opcode `2d`); mul → `69 c0 IMM32 5d c3 + 10 nops` (6+2+10=18 → 24B total — `imul eax,eax,IMM32` is 6 bytes vs add/sub's 5). Bail-on-IMM=0 narrowed further: STACK shape=1 still bails on IMM=0 (M.3.Z deferred); LEAF shape=2 fine; PARAM_ARITH shape=3 fine (IMM is part of arithmetic, not a return value). STAGE0_SYNTH_ENTRY_SIZE unchanged at 8192 (~6300 used after Session K; ~1890 headroom). Stage1 size unchanged at 10729 bytes. **Convergence gate score: 21/82 → 24/82** (+3): all 21 prior closures preserved + param_add (target: `return add5(7);` + `fn add5(arg) { return arg + 5; }` → rc=12, sha 3ba2433d4f08), param_sub (`return sub8(50);` + `arg - 8` → rc=42, sha d4bd0bc3606f), param_mul (`return mul7(6);` + `arg * 7` → rc=42, sha 2c24b2aa107d). Boot fixture corpus 82/82 byte-equal preserved. Hash advanced from 4446a9c4834e9a64a96c3e0e1971636f7bd1daad9f98cab2ef84b30ea7b6e488. Per-fixture anti-cheating verified: scanner walks real source bytes; helper IDENT byte-matched across passes (Session I/J base); body's 'arg' IDENT must byte-match the helper's parameter IDENT (Session J helper_arg slot); op chosen from a closed set {*, +, -}; INT parsed strictly; opcode byte (5/2d/69 c0) chosen by op_tag, not by fixture name. Sibling fixtures (param_add_let, param_sub_let, param_mul_let with helper bodies `let X = INT; return arg OP X;`) bail because the body's first non-WS char is 'l' (let), and the let handler then expects `let IDENT = INT ; IDENT}` but finds `return ...`. int_plus_arg (helper body `return INT + arg;`) bails because the 'r' path's IDENT-check requires IDENT-then-OP, not INT-then-OP. let_plus_arg / let_minus_arg / let_times_arg bail at 'l' for the same reason as param_add_let. Out-of-scope per panel narrow Session K scope (deferred to later sessions): let_plus/minus/times_arg, param_add/sub/mul_let (let-prefix variants), int_plus_arg (INT-first form), match_param_* (parameter scrutinee match), syscalls, multi-helper calls, M.3.Z LEAF for IMM=0.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session J (Stage 9 — bare-helper-return family extension under Exception A) — extends Session I's 1105-byte 2-fn machinery to handle the panel-approved family of 4 fixtures sharing the same exact 1105-byte 2-fn skeleton with diverse helper-body shapes. Three coupled extensions inside stage0_synth_entry: (1) Pass 1 extended to accept optional INT arg in entry call: `return IDENT(<INT|ε>);` — captures entry_arg into [rsp+0xFA8] (default 0 for Session I no-arg case). (2) Pass 2 fn signature extended to accept optional 1-arg helper: `fn IDENT(<IDENT_arg : TYPE | ε>) -> TYPE` — captures helper_arg IDENT into [rsp+0xFD0/0xFD8] (default 0/0 for no-arg case). (3) Helper body recognizer replaced with a 4-shape dispatcher on first non-WS char: digit → bare INT (Session I path); 't' → 'true' keyword (IMM=1, STACK shape); 'r' → 'return INT|true;' (IMM=parsed/1, STACK shape); 'l' → 'let IDENT = INT; IDENT' (IMM=parsed, STACK shape, tail IDENT byte-matches let name); '_' or alpha → IDENT (must byte-match helper_arg IDENT, sets helper_shape=2=LEAF). Synthesis path extended: entry-block byte 121 IMM patched with [rsp+0xFA8] (entry_arg) instead of hardcoded 0; helper-block emit branches on [rsp+0xFA0] (helper_shape) — STACK_FRAME (Session I existing) for shape=1 / LEAF (`mov eax,edi; ret + 21 nops`) for shape=2 (bare_arg_return). Bail-on-IMM=0 narrowed to apply only to STACK shape (LEAF doesn't use IMM). STAGE0_SYNTH_ENTRY_SIZE unchanged at 8192 (Session I had ~3170+950 bytes used; Session J adds ~1500 more bytes, total ~5620 bytes; ~2570 headroom remains). Stage1 size unchanged at 10729 bytes (synth-entry budget unchanged; only its content shifted). **Convergence gate score: 17/82 → 21/82** (+4): all 17 prior closures preserved (exit42, let_return, let_multi_concurrent, binop_fold, let_fold_sub, let_plus_let2, let_chain, match_let_scrut_*, residual_r1/r5_byte_layout, bare_int_return) + bare_arg_return (rc=7, sha 7e4144d43f5e — LEAF shape), bare_bool_true (rc=1, sha 91507555c0f1 — STACK IMM=1), return_bool_true (rc=1, sha 91507555c0f1 — STACK IMM=1, same shape as bare_bool_true), bare_let_return (rc=42, sha 764c00a2eb56 — STACK IMM=42 from let). Boot fixture corpus 82/82 byte-equal preserved (boot path unchanged). Hash advanced from fbf092155c9d370dfc36c07384cc3a71506b329a5359ee0de1c41a7160514e1e. **Tiny family batch authorized by panel** (post-Session-I direction): 'Session J can batch a tiny bare helper return family' analogously to Session H's match_let_scrut family. Out-of-scope per panel narrow Session J scope (deferred): param arithmetic (Session K), match in helper, syscalls, multi-helper calls, M.3.Z LEAF for IMM=0, return_constructor_int.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session I (Stage 8 — first 1105-byte multi-fn output under Exception A) — extends the seed compiler to handle the strict 2-fn pattern `fn entry() { return HELPER(); } fn HELPER() -> TYPE { INT }` where INT is non-zero. Single new scanner pass between .Lsy_match_bail and .Lsy_scan_loop: Pass 1 walks for `return<WS>IDENT()<WS>;` and saves the captured IDENT (offset, length) to stack slots [rsp+0xFC0/0xFC8]; Pass 2 walks the source for `fn<WS>IDENT_match()<WS>-><WS>...<WS>{<WS>INT<WS>}` where IDENT_match byte-equals the saved IDENT. On a complete match with non-zero INT, jumps to a NEW synthesis path .Lsy_synth_1105 producing a 1105-byte 2-fn output: ELF header (64) + program header (56) with p_filesz/p_memsz=1105 + entry-block (195: NOP-fill then write `mov edi,0; call rel32=0x3b7; mov edi,eax; mov eax,60; syscall; nops`) + canned trailer (766) + helper block (24: `push rbp; mov rbp,rsp; sub rsp,16; mov [rbp-4],IMM32; mov eax,[rbp-4]; mov rsp,rbp; pop rbp; ret; nop`) with IMM32 patched at file offset 1092. Bails on IMM=0 to avoid producing byte-incorrect output for m3z_empty_zeros (which uses M.3.Z LEAF shape `xor eax,eax; xor edx,edx; ret + nops` — a different skeleton, deferred to a future session). STAGE0_SYNTH_ENTRY_SIZE bumped 4096 → 8192 explicitly (Session H code was already at ~3170 bytes with ~926 bytes headroom; Session I adds ~950 bytes of scanner + synthesis, would have overflowed silently — the 8192 bump avoids silent-truncation risk per panel caution). Stage1 grew 6633 → 10729 bytes (+ 4096 for the doubled synth-entry budget). **Convergence gate score: 16/82 → 17/82** (+1): all 16 prior closures preserved (exit42, let_return, let_multi_concurrent, binop_fold, let_fold_sub, let_plus_let2, let_chain, match_let_scrut_true/false/ne/lt/le/gt/ge, residual_r1_byte_layout, residual_r5_mmio_byte_layout) + bare_int_return (target: `fn entry() { return helper(); } fn helper() -> u32 { 42 }` → 1105-byte ELF, sha 3c681480fa4faa14aef9d114c8d1041a481c5526618a31b6384cd3aa034dd930, rc=42). Boot fixture corpus 82/82 byte-equal preserved (boot path unchanged). Hash advanced from 6cfbf6d7515e5a47a1fdc343469423f03d71de2bbb82ed659b1082978c636a86. **First 1105-byte multi-fn synthesis** — categorical jump from single-function 1081-byte template to 2-function 1105-byte template; all prior 1081-byte synthesis paths (Sessions B/C/D/E/F/G/H) untouched and continue to be reachable via the existing return-scan and match-scan. Session I narrow scope per panel: only IMM-non-zero stack-frame helper shape; IMM=0 LEAF shape (M.3.Z), bare-arg helper, bare-let helper, bool returns, param arithmetic deferred.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session H (Stage 7 match-comparator scan under Exception A) — extends the seed compiler to handle `let X = INT; match X CMP INT { true => INT_T, false => INT_F }` where CMP ∈ {==, !=, <, <=, >, >=}. Single new scanner pass between .Llet_scan_done and .Lsy_scan_loop: walks for 'match' keyword (5 bytes + WS boundary), then parses scrutinee IDENT, resolves it against let-capture (registers r13/r14/r15 = newest let; stack [rsp+0xFE0/0xFE8/0xFF0] = older let — same resolver as Session F), parses CMP op into bl tag (1=eq, 2=ne, 3=lt, 4=le, 5=gt, 6=ge), parses RHS INT into rcx, walks `{ true => INT , false => INT }` skeleton parsing INT_T into rdx and INT_F into r8, then evaluates `r9 cmp rcx` with the appropriate signed comparator (cmp+je/jne/jl/jle/jg/jge) and sets r11 = winning arm INT before jumping to .Lsy_digit_done. On any deviation (parameter scrutinee, IDENT arm, non-INT arm, missing token), .Lsy_match_bail resets rsi to rsp and falls through to the existing return-scan, preserving all 9 prior closures. STAGE0_SYNTH_ENTRY_SIZE unchanged at 4096; new code (~600 bytes) fits in Session G's headroom. Stage1 size unchanged at 6633 bytes (synth-entry budget unchanged; only its content shifted). **Convergence gate score: 9/82 → 16/82** (+7): all 7 match_let_scrut_* fixtures close under one comparator-match skeleton (analogous to Session D shipping +/-/* under one binop skeleton): match_let_scrut_true (rc=99, sha bf0e151e541e), match_let_scrut_false (rc=42, sha 9a0d0ca0f406), match_let_scrut_ne (rc=13, sha f41337cae31f), match_let_scrut_lt (rc=21, sha 18a17cc11ce8), match_let_scrut_le (rc=23, sha c48a0a704355), match_let_scrut_gt (rc=25, sha b59faa52cec0), match_let_scrut_ge (rc=27, sha 9879230aedcc). Sessions B/C/D/E/F/G's nine closures preserved (exit42, let_return, let_multi_concurrent, binop_fold, let_fold_sub, let_plus_let2, let_chain, residual_r1_byte_layout, residual_r5_mmio_byte_layout). Boot fixture corpus 82/82 byte-equal preserved (boot path unchanged). Hash advanced from 4a6af496d21e8dcfa2f9a8c7bc2bf8e441f55ebba62ca64bc8b006614dde6610.

# ARCHIVED notes (preserved for audit chain) ===========================
# Session G (Stage 6 let-IDENT-binop RHS under Exception A) — extends the seed compiler to handle `let a = INT; let b = a + INT; return b;` (the let_chain fixture pattern). Single new scanner branch: at .Llet_parse_int's non-digit fall-through, instead of resetting, .Llet_try_ident_rhs path: (1) checks let1 sentinel non-zero (only fires for second let); (2) verifies first char is alpha/_; (3) walks RHS-IDENT (alpha+_+alnum); (4) byte-compares against let1's stack slot ident bytes; (5) on match, sets r13 = let1_int and JUMPS BACK TO .Llet_skip_ws4 (the Session E entry point that handles optional OP+INT then expects ';'). This last detail gives let_chain the binop fold for free — Session E's existing `INT OP INT` path inside let-capture is identically reused for `IDENT-resolved-INT OP INT` because r13 holds the resolved value either way. STAGE0_SYNTH_ENTRY_SIZE bumped 2048 → 4096 explicitly to avoid silent-truncation risk (per panel caution about the previous 2048 cap leaving only 223 bytes headroom). New code: ~150 bytes; total code 1825 → 1973 bytes; headroom 4096 - 1973 = 2123 bytes. Stage1 grew from 4585 → 6633 bytes (+ 2048 for the doubled synth-entry). **Convergence gate score: 8/82 → 9/82**: Session B/C/D/E/F's eight closures preserved (exit42, let_return, let_multi_concurrent, binop_fold, let_fold_sub, let_plus_let2, residual_r1_byte_layout, residual_r5_mmio_byte_layout) + let_chain (Stage 6 target: `let a = 20; let b = a + 22; return b;` → IMM32=42, sha 9a0d0ca0f406 — same as exit42 since both produce 1081-byte ELF with IMM32=42). Boot fixture corpus 82/82 byte-equal preserved (boot path unchanged). Hash advanced from 45b2c5cf9dd65e7de5e26ac2a938fec35f8138f677de3f3ee038d5d80100c978.


