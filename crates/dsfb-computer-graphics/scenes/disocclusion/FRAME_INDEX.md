# Frame Index — Disocclusion

Frames selected from existing capture:

- frame_0001 — pre-disocclusion baseline (occluded region fully hidden)
- frame_0002 — disocclusion onset (background exposed, history invalid)
- frame_0003 — mismatch persistence / colour smear visible
- frame_0004 — partial history decay, residual reducing
- frame_0005 — recovery toward stable accumulation

These references point to:

materialized_external/frame_*/

No duplication of data is performed.
