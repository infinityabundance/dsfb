# docs/ — dsfb-chemical-engineering-core

Crate-level documentation pointer. Embedded grammar (no_std, no-heap, fixed-point): the residual triple + ring buffer + admissibility envelope + grammar state machine in scaled integers. Primary references: the crate `README.md`, `ARCHITECTURE.md`,
`SAFETY.md`, `SECURITY.md`, the workspace `docs/`, and the audit posture in `../../../audit/`.
