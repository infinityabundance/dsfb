# docs/ — dsfb-chemical-engineering-edge

Crate-level documentation pointer. CPU execution: the residual pipeline, drift/slew/envelope grammar, detector bank, deterministic quorum fusion, heuristics, reports/figures, and the Chemical Court Record v1 bundle. Primary references: the crate `README.md`, `ARCHITECTURE.md`,
`SAFETY.md`, `SECURITY.md`, the workspace `docs/`, and the audit posture in `../../../audit/`.
