# docs/ — dsfb-chemical-engineering-wasm

Crate-level documentation pointer. Browser what-if Chemical Court simulator: replays a residual stream under an operator-amended admissibility envelope over immutable evidence (raw extern "C" exports, no wasm-bindgen). Primary references: the crate `README.md`, `ARCHITECTURE.md`,
`SAFETY.md`, `SECURITY.md`, the workspace `docs/`, and the audit posture in `../../../audit/`.
