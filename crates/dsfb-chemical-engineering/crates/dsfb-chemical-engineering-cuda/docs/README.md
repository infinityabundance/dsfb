# docs/ — dsfb-chemical-engineering-cuda

Crate-level documentation pointer. GPU evidence factory + forensic court: on-GPU SHA-256 + Merkle evidence root, byte-exact to the CPU reference, digest-equivalence-gated. Primary references: the crate `README.md`, `ARCHITECTURE.md`,
`SAFETY.md`, `SECURITY.md`, the workspace `docs/`, and the audit posture in `../../../audit/`.
