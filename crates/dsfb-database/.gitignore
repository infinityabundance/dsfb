# Crate-local Cargo build artefacts
target/

# Reproducibility outputs (figures, CSVs, JSON metrics)
out/

# Operator-supplied real data (Snowset shards, real pg_stat_statements
# dumps) — never commit. The bundled redacted sample under
# `examples/data/` is the only data file that ships with the crate.
/data/
!examples/data/

# LaTeX build artefacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.toc
paper/*.dvi
paper/*.ps
paper/dsfb-database.pdf

# Colab notebook execution artefacts
.ipynb_checkpoints/

# Editor / OS junk
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
