================================================================
T.12.h - Data Quality / Tabular / Database Integrity Constraints
        regression-check receipt (10-step per-section ritual)
================================================================

Step 1. cargo fmt --all --check
  Result: clean.

Step 2. cargo clippy --workspace --all-targets --features cuda -- -D warnings
  Result: clean.

Step 3. bash scripts/scrub.sh
  Result: clean.

Step 4. bash scripts/docs_freshness.sh
  Result: clean.

Step 5. cargo test -p dsfb-gpu-atlas-corpus
  Result: 1003 tests pass (T.12.g baseline 962 + 41 new T.12.h
  acceptance tests). Two initial test failures (Rust string-
  literal continuation joins with whitespace, so the actual
  in-memory reason text contains "training- data anchor" not
  "training-data anchor" and "new-category- appearance" not
  "new-category-appearance"); fixed by relaxing the test
  assertions to match the actual continuation pattern. The
  source reason text is unchanged so the three emitted hashes
  are byte-stable from first emit.

Step 6. cargo test --workspace --features cuda -- --test-threads=1
  Result: 73 test groups, 1318 tests PASS, 0 FAIL. Delta from
  T.12.g seal (f2622e3): +1 test group, +41 tests.

Step 7. tests/r12_d64_saturation regression check
  Result: pinned episode counts byte-stable (13/89/1917).
  Pinned R.12b baseline NOT rebaselined.

Step 8. Stale-doc scan
  Result: clean. README.md, archived prior-art manuscript source, lib.rs
  module docstring, and main.rs usage block all refreshed
  through T.12.h. New T.12.h section in README between T.12.g
  and the scaling ladder.

Step 9. README / plan refresh
  Result: README T.12.h section + front-door + Layout +
  Paper-section refreshed from T.1-T.12.g to T.1-T.12.h.
  Plan-file post-commit will mark T.12.h sealed.

Step 10. Atomic commit
  Single atomic commit landing:
    - src/t12_h_dataquality.rs (new, ~770 lines fully commented)
    - src/lib.rs (module export + module docstring refresh)
    - src/main.rs (2 CLI subcommands + usage block)
    - tests/t12_h_dataquality_invariants.rs (new, 41 tests
      including 11 panel-required load-bearing negatives)
    - out/t12_h_dataquality_proposal_v1.{txt,json} (new)
    - reports/t12_h_dataquality_proposal_summary.txt (new)
    - reports/t12_h_dataquality_proposal_verification.txt (new)
    - reports/t12_h_regression_check.txt (new)
    - README.md (T.12.h section + front-door refresh)
    - archived prior-art manuscript source (T.12.h bullet)
    - .gitignore (3 new whitelist lines)
  No --no-verify. Pre-commit hook gates pass.

Hash posture:
  corpus_hash_v1                              : unchanged (35c276c7...)
  registry_hash_v2                            : unchanged (d3cf6300...)
  Every prior T.11/S1.3/T.12.0-T.12.g hash    : unchanged
  Every DetectorPassport hash                  : unchanged
  SEED.len()                                  : 54 (unchanged)
  T.12.h data-quality literature_expansion_batch_hash_v1 (NEW) :
    6e77d173bc12f3e6147297e5981800cb7c6883ada039d3ea40df23373fcde0ae
  T.12.h data-quality dedup_court_delta_hash_v1 (NEW) :
    568719a54af6a7e93d9bc381328eb929a43ab90324d70f1e20cad5c3f85f9e70
  T.12.h data-quality corpus_amendment_proposal_hash_v1 (NEW) :
    d4566a74b0816d4e1bd4612fb7ee677ff5ae9a679af44b1bcdacab670f9779da

Doctrine constraints preserved:
- No probability, no learned weights, no fast-math, no atomics
  for accumulation.
- Semantic Non-Bypass Axiom intact.
- Apache-2.0 + Invariant Forge LLC Background IP notice; no
  MIT regression.
- No attribution-string regressions.
- Corpus crate stays host-only and zero-dep.
- no-silent-court-logic doctrine: every pub item AND every
  private helper in t12_h_dataquality.rs carries a doc comment
  whose first sentence states the WHY for a future engineer.
- Scope + decision-law contract discipline: every Canonical
  Addition and ExistingCanonicalAuthorityResolution record
  declares its specific scope + baseline + null-semantics +
  type-system / key-scope / range-bound / association-law +
  decision-law contract.
- Target-leakage non-claim: target-leakage candidate (5803) is
  admitted as CANDIDATE witness, not proof; future activation
  planner / case-file emitter MUST NOT promote candidate
  signals into ratified leakage verdicts.
- Learned-DQ-score and auto-schema-inference honesty: both
  RejectedNotDeterministic until model-identification anchor
  / inference algorithm contracts are declared.

T.12.h is sealed. The amendment court has admitted the eighth
real expansion proposal: 8 new canonical data-quality / tabular
/ integrity primitives, 5 existing-canonical cross-class
authority resolutions, 1 DomainTransferOf, 3 ParameterizationOf
records, 2 RejectedNotDeterministic records, 10 genealogy
edges, 9 source refs - all WITHOUT mutating SEED or
corpus_hash_v1.

Next campaign (panel-authorized post-T.12.h): T.12.i -
Observability / Debugging. Same SEED-walk-first discipline.
Latency ramp, error burst, slew shock, fanout cascade, route-
local anomaly, service-local anomaly, retry storm, queue-depth
pressure, saturation precursor, cold-start transient. Note
that several of these (latency ramp, error burst, slew shock,
fanout cascade) are likely already canonical in SEED as the
dsfb-gpu-debug bank surface.
