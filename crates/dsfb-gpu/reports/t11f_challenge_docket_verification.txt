================================================================
DSFB-GPU-Atlas — T.11f Challenge Docket verification
================================================================
Suite          : tests/challenge_docket_invariants.rs
Tests          : 62 (PASS)
Verifier kinds : 17 (each exercised by ≥1 test)
Seed entries   : 10 (panel-locked conservative seed)

----------------------------------------------------------------
Test invocation
----------------------------------------------------------------
$ cargo test -p dsfb-gpu-atlas-corpus --test challenge_docket_invariants

  running 62 tests
  test result: ok. 62 passed; 0 failed; 0 ignored; 0 measured

----------------------------------------------------------------
Schema + constants (5 tests)
----------------------------------------------------------------
  - challenge_docket_domain_separator_is_panel_locked
        CHALLENGE_DOCKET_DOMAIN == "DSFB-GPU-ATLAS:CHALLENGE-DOCKET:v1\0"
  - challenge_docket_schema_id_is_panel_locked
        CHALLENGE_DOCKET_SCHEMA_V1 == "DSFB-GPU-ATLAS:CHALLENGE-DOCKET:v1"
  - collected_snapshot_matches_static_seed_length
        collect_challenge_docket().challenges.len() == 10
  - collected_snapshot_is_sorted_by_challenge_id_ascending
  - collected_snapshot_schema_is_v1

----------------------------------------------------------------
Hash determinism + sensitivity (12 tests)
----------------------------------------------------------------
  - docket_hash_is_deterministic_across_two_builds
  - docket_hash_changes_when_entry_added
  - docket_hash_changes_when_claim_changes
  - docket_hash_changes_when_status_changes
  - docket_hash_changes_when_kind_changes
  - docket_hash_changes_when_target_changes
  - docket_hash_changes_when_severity_changes
  - docket_hash_changes_when_evidence_added
  - docket_hash_changes_when_court_response_changes
  - docket_hash_changes_when_proposed_resolution_changes
  - docket_hash_changes_when_affected_hash_set_changes
  - docket_hash_is_independent_of_entry_order

The "independent of entry order" test asserts the hash function
sorts entries by `challenge_id` before hashing, so a hand-built
snapshot with reversed order produces the same hash.

----------------------------------------------------------------
Positive admission paths (3 tests)
----------------------------------------------------------------
  - verifier_admits_clean_seed_docket
        verify_challenge_docket(&collect_challenge_docket())
        returns an empty error vector — the 10-entry seed is
        admissible.
  - verifier_admits_singleton_overruled_overruled_with_reason
        A single overruled entry with reason text is admissible.
  - verifier_admits_open_critical_when_proposed_deferral_present
        An Open + Critical entry MAY exist when the operator has
        proposed DeferToFutureCommit; this admits the conservative
        path for filing a critical objection before the court
        responds.

----------------------------------------------------------------
Verifier-reject coverage — one test per kind (17 reject kinds)
----------------------------------------------------------------
Two PANEL-REQUIRED load-bearing negatives:

  1. challenge_docket_rejects_sustained_challenge_without_resolution
        Sustained + ProposedResolution::NoAction
        → SustainedWithoutResolution
        (panel-locked: a Sustained challenge MUST carry a
         proposed resolution; otherwise the docket would mark
         a corpus mutation as required without naming it)

  2. challenge_docket_rejects_bad_source_without_source_evidence
        ChallengeKind::BadSource without SourceRef / SourceHash
        evidence
        → BadSourceWithoutSourceEvidence
        (panel-locked: filing a BadSource objection without
         pointing at an actual SourceRef or SourceHash is a
         claim without evidence)

The remaining 15 reject kinds each have a dedicated test:

  3. verifier_rejects_challenge_against_missing_detector
        Detector(99_999) → ChallengeAgainstMissingDetector
  4. verifier_rejects_challenge_against_missing_passport
        Passport(99_999) → ChallengeAgainstMissingDetector
  5. verifier_rejects_challenge_against_missing_precedent
        Precedent(99_999) → ChallengeAgainstMissingPrecedent
  6. verifier_rejects_challenge_against_missing_grammar_rule
        GrammarRule(99_999) → ChallengeAgainstMissingGrammarRule
  7. verifier_rejects_empty_claim
        claim == "" → EmptyClaim
  8. verifier_rejects_empty_created_in_stage
        created_in_stage == "" → EmptyChallenger
  9. verifier_rejects_duplicate_challenge_id
        Two entries with ChallengeId(42) → DuplicateChallengeId
 10. verifier_rejects_overruled_with_not_yet_responded
        Overruled + NotYetResponded → OverruledWithoutCourtResponse
 11. verifier_rejects_overruled_with_empty_reason_text
        Overruled + OverruledReason("") → OverruledWithoutCourtResponse
 12. verifier_rejects_deferred_with_not_yet_responded
        Deferred + NotYetResponded → DeferredWithoutDeferralReason
 13. verifier_rejects_deferred_with_empty_gate_text
        Deferred + DeferredToGate("") → DeferredWithoutDeferralReason
 14. verifier_rejects_superseded_with_not_yet_responded
        Superseded + NotYetResponded → SupersededWithoutCommitReference
 15. verifier_rejects_superseded_with_empty_commit_reference
        Superseded + SupersededByCommit("") → SupersededWithoutCommitReference
 16. verifier_rejects_open_with_court_response
        Open + OverruledReason(...) → StatusResponseInconsistent
 17. verifier_rejects_runtime_too_high_without_runtime_evidence
        RuntimeTooHigh without RuntimeCostUs → RuntimeTooHighWithoutRuntimeEvidence
 18. verifier_rejects_formula_mismatch_without_formula_hash
        FormulaMismatch without FormulaHash → FormulaMismatchWithoutFormulaHashReference
 19. verifier_rejects_wrong_witness_role_without_semantic_role_hash
        WrongWitnessRole without SemanticRoleHash → WrongWitnessRoleWithoutSemanticRoleHashReference
 20. verifier_rejects_unimplemented_but_claimed_against_honest_l_band
        UnimplementedButClaimed targeted at a detector whose
        L-band is already L0..L4 → UnimplementedButClaimedAgainstHonestLBand
        (panel-locked: an "unimplemented but claimed" objection
         only makes sense against a detector that DOES claim L5
         or L6; filing it against an L0/L1/L2 record is a no-op)
 21. verifier_rejects_open_critical_without_deferred_gate
        Open + Critical + affected_hashes != ∅ +
        ProposedResolution::NoChangeRequired → OpenCriticalWithoutDeferredGate

(20 + 21 are paired: 21 is the load-bearing positive that
shows the conservative deferral path is admissible.)

----------------------------------------------------------------
Target-kind + ChallengeKind + Status + Severity wire-name
stability (4 tests)
----------------------------------------------------------------
  - target_kind_strings_are_stable
  - challenge_kind_strings_are_stable
  - status_strings_are_stable
  - severity_strings_are_stable

These pin every enum's `as_str()` to its panel-locked wire name
so future code refactors cannot silently change the hash material.

----------------------------------------------------------------
Rendering (7 tests)
----------------------------------------------------------------
  - render_text_is_deterministic_across_two_calls
  - render_json_is_deterministic_across_two_calls
  - render_text_carries_docket_hash
  - render_json_carries_docket_hash
  - render_text_carries_panel_locked_non_claim
        text contains "NOT a corpus mutation"
  - render_json_contains_every_entry
  - render_text_lists_every_overruled_reason
  - hash_is_not_a_function_of_rendered_text_bytes

The renderers are byte-stable across two calls. The hash is over
canonical bytes only — rendered text + JSON are derived but not
included in the hash material.

----------------------------------------------------------------
Seed-shape invariants (8 tests; the conservative-seed pins)
----------------------------------------------------------------
  - seed_has_no_open_challenges
        Panel-locked T.11f posture: no Open entries in the seed.
  - seed_has_no_sustained_challenges
        Panel-locked T.11f posture: no Sustained entries in the
        seed. Sustaining a challenge requires a SEPARATE later
        commit that mutates the canonical artifact.
  - seed_overruled_entries_carry_overruled_reason_variant
        Every Overruled entry carries `CourtResponse::OverruledReason`
        with non-empty reason text.
  - seed_deferred_entries_carry_deferred_to_gate_variant
        Every Deferred entry carries `CourtResponse::DeferredToGate`
        with non-empty gate text.
  - seed_every_entry_was_created_in_t11f
        created_in_stage == "T.11f" for every entry.
  - seed_runtime_too_high_entries_carry_runtime_evidence
  - seed_bad_source_entries_carry_source_evidence
  - affected_hash_set_default_is_all_false
  - affected_hash_set_not_empty_when_any_field_true

----------------------------------------------------------------
Hash chain integrity (no prior anchor mutated)
----------------------------------------------------------------
T.11f does NOT touch:

  - corpus_hash_v1                 (T.10)
  - registry_hash_v2               (S1.2)
  - precedent_hash_v1              (T.11b)
  - admissibility_grammar_hash_v1  (T.11c)
  - trial_transcript_hash_v1       (T.11d)
  - receipt_hash_v1                (T.11e)
  - any DetectorPassport hash      (T.11a)

T.11f's new challenge_docket_hash_v1 lives in its own namespace
(domain separator DSFB-GPU-ATLAS:CHALLENGE-DOCKET:v1\0). The
docket records adversarial overlay metadata; it never alters
the upstream chain.

----------------------------------------------------------------
Bulk-emit invocation
----------------------------------------------------------------
$ cargo run -p dsfb-gpu-atlas-corpus -- challenges-emit \
      --out-dir crates/dsfb-gpu-atlas-corpus/out

  wrote crates/dsfb-gpu-atlas-corpus/out/challenge_docket_v1.txt (2973 bytes)
  wrote crates/dsfb-gpu-atlas-corpus/out/challenge_docket_v1.json (8673 bytes)

challenge_docket_hash_v1 =
    dde4ecb4de491f93fe671b77d6ffd8567199f6d1a3f5cca51ea4facb21c10415

----------------------------------------------------------------
Final verification status
----------------------------------------------------------------
  - 62 / 62 acceptance tests PASS.
  - All 17 verifier-reject kinds exercised by dedicated tests
    (two panel-required load-bearing negatives explicitly named).
  - All wire-name strings pinned.
  - Renderers byte-stable; hash independent of rendered output.
  - Conservative seed posture pinned (0 Open, 0 Sustained, 6
    Overruled, 4 Deferred, 0 Superseded).
  - Bulk-emit artifacts byte-stable across two invocations.
  - No upstream hash anchor disturbed.
  - The docket is an adversarial overlay; mutating any canonical
    artifact requires a separate later commit.
