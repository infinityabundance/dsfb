================================================================
T.12.k - Industrial / Fault Detection and Diagnostics /
        Condition Monitoring
        regression-check receipt (10-step per-section ritual)
================================================================

Step 1. cargo fmt --all --check
  Result: clean.

Step 2. cargo clippy --workspace --all-targets --features cuda -- -D warnings
  Result: clean.

Step 3. bash scripts/scrub.sh
  Result: clean.

Step 4. bash scripts/docs_freshness.sh
  Result: clean.

Step 5. cargo test -p dsfb-gpu-atlas-corpus
  Result: 37 groups / 1145 tests pass (T.12.j baseline 36 /
  1096 + 1 new group + 49 new T.12.k acceptance tests).
  Initial run had 4 test failures: 2 on the Rust string-
  literal continuation whitespace gotcha ("autocorrelation- "
  + "of-innovations" and "operating- " + "regime set"); 1 on
  "maintenance recommendation" being in the forbidden-term
  list while also appearing legitimately in the non-claim
  phrase; 1 on the plant-or-residual term list being too
  narrow (didn't accept "envelope" / "estimator" /
  "computation" for spectral / CI / derived-condition
  detectors). Fixed by relaxing the assertions (multi-
  substring matching for the two continuation-split phrases,
  dropping "maintenance recommendation" from forbidden list
  since the non-claim phrase legitimately uses it as a
  disclaimer, broadening plant-or-residual terms to include
  the full math-structure vocabulary the FDD source class
  uses). Source reason-text bytes unchanged; three emitted
  hashes byte-stable from first emit.

Step 6. cargo test --workspace --features cuda -- --test-threads=1
  Result: 76 test groups, 1460 tests PASS, 0 FAIL. Delta from
  T.12.j seal (1ae99fd): +1 test group, +49 tests.

Step 7. tests/r12_d64_saturation regression check
  Result: pinned episode counts byte-stable (13/89/1917).
  Pinned R.12b baseline NOT rebaselined.

Step 8. Stale-doc scan
  Result: clean. README.md, archived prior-art manuscript source, lib.rs
  module docstring, and main.rs usage block all refreshed
  through T.12.k. New T.12.k section in README between
  T.12.j and the scaling ladder.

Step 9. README / plan refresh
  Result: README T.12.k section + front-door + Layout +
  Paper-section refreshed from T.1-T.12.j to T.1-T.12.k.
  Plan-file post-commit will mark T.12.k sealed and rotate
  next-active campaign to T.12.l.

Step 10. Atomic commit
  Single atomic commit landing:
    - src/t12_k_industrial.rs (new, ~830 lines fully commented)
    - src/lib.rs (module export + module docstring refresh)
    - src/main.rs (2 CLI subcommands + usage block)
    - tests/t12_k_industrial_invariants.rs (new, 49 tests
      including 16 panel-required load-bearing negatives,
      the most-important plant-or-residual contract scanner,
      and the root-cause-claim-language scanner)
    - out/t12_k_industrial_proposal_v1.{txt,json} (new)
    - reports/t12_k_industrial_proposal_summary.txt (new)
    - reports/t12_k_industrial_proposal_verification.txt (new)
    - reports/t12_k_regression_check.txt (new)
    - README.md (T.12.k section + front-door refresh)
    - archived prior-art manuscript source (T.12.k bullet)
    - .gitignore (3 new whitelist lines)
  No --no-verify. Pre-commit hook gates pass.

Hash posture:
  corpus_hash_v1                              : unchanged (35c276c7...)
  registry_hash_v2                            : unchanged (d3cf6300...)
  Every prior T.11/S1.3/T.12.0-T.12.j hash    : unchanged
  Every DetectorPassport hash                  : unchanged
  SEED.len()                                  : 54 (unchanged)
  T.12.k industrial literature_expansion_batch_hash_v1 (NEW) :
    5c952a60f4f51fec2c28b49b268638d887a7a360eccf1f30506a4bdf99b562bf
  T.12.k industrial dedup_court_delta_hash_v1 (NEW) :
    3c32085d5d333c39d3d77e8fb8885a6499ceb48e322b10d4c71aeae5c70105fa
  T.12.k industrial corpus_amendment_proposal_hash_v1 (NEW) :
    22f06c769daad5472255edce97cbe9e419b5aa22c6d07345a5c667c23ce93565

Doctrine constraints preserved:
- No probability, no learned weights, no fast-math, no atomics
  for accumulation.
- Semantic Non-Bypass Axiom intact.
- Apache-2.0 + Invariant Forge LLC Background IP notice; no
  MIT regression.
- No attribution-string regressions.
- Corpus crate stays host-only and zero-dep.
- no-silent-court-logic doctrine: every pub item AND every
  private helper in t12_k_industrial.rs carries a doc comment
  whose first sentence states the WHY for a future engineer.
- Panel "success shape" applied: lean toward cross-class dedup
  discipline (8 authority resolutions) rather than canonical
  count (only 6 new primitives). 8 panel-suggested candidates
  reduced to 6 via SEED-walk-first collapse of bearing
  vibration / motor current signature / temperature envelope
  / pressure transient into ParameterizationOf records of
  existing SEED canonicals.
- Plant-or-residual contract discipline: every CanonicalAddition
  reason declares math-structure + decision-functional contract.
- Root-cause-claim discipline (MOST IMPORTANT): every
  CanonicalAddition / ExistingCanonicalAuthorityResolution
  reason is scanned for forbidden root-cause / RUL / failure-
  mode-classification terms; forbidden terms appear ONLY in
  RejectedNotDeterministic reason text. Every qualifying
  reason ends with the panel-locked non-claim "condition-
  monitoring witness, not a maintenance recommendation".
- Proprietary-PdM and learned-fault-classifier honesty: both
  RejectedNotDeterministic until model-identification anchor /
  training-data anchor / deterministic-formula contracts are
  declared.

T.12.k is sealed. The amendment court has admitted the
eleventh real expansion proposal: 6 new canonical FDD
primitives, 8 existing-canonical authority resolutions (the
largest SEED-collision ratification of any T.12.x to date),
2 DomainTransferOf records (SEED 12 FFT band-energy as shared
spectral ancestor + SEED 22 Residual envelope exit as shared
envelope-boundary ancestor for FaultDetectionDiagnostics), 4
ParameterizationOf records, 2 RejectedNotDeterministic records
(fifth T.12.x with two rejections), 12 genealogy edges, 9
source refs - all WITHOUT mutating SEED or corpus_hash_v1 and
WITHOUT admitting a single root-cause / maintenance /
failure-mode claim.

Next campaign (panel-authorized post-T.12.k): T.12.l -
Chemometrics (per corrected T.12 source-class sequence in
Panel-audit deferred items section A.1; reserved id band
6201..=6299). Same SEED-walk-first discipline. Candidate
primitives: Mahalanobis distance on PCA / PLS scores
(possibly already-canonical adjacency via SEED 19 / 20 / 21),
calibration drift, leverage outlier, latent-variable control
chart variants, concentration drift, spectroscopy-baseline
correction artifact. Note that PCA T² (SEED 19), PCA SPE/Q
(20), and PLS residual (21) are already canonical from
earlier SEED + T.12.k — chemometrics SEED-walk should
authority-resolve those.
