# Post-T.1b regression check (per-section discipline artifact)
#
# Hardware:        RTX 4080 SUPER
# CUDA toolkit:    13.2
# Pre-T.1b head:   375cff9 (T.1b corpus seed expansion commit)
# Test:            tests/r12_d64_saturation.rs (D64 saturation sweep)
# Method:          re-run the R.12b saturation matrix after T.1a + T.1b
#                  landed; compare against the pinned R.12b money table
#                  in reports/money_table.txt and reports/r12_d64_saturation.txt.
#
# Why this artifact exists:
#   T.1a + T.1b are host-only schema commits adding the dsfb-gpu-atlas-
#   corpus crate. They do not touch any GPU code path, so a "performance
#   delta" in the strict R-campaign sense (kernel-time change) is not
#   measurable. What CAN regress is the semantic invariants the existing
#   GPU paths depend on: episode counts, byte-identical case files, and
#   end-to-end test pass. This artifact records that those held after
#   T.1b landed.
#
# Result: NO REGRESSION.
#   - Test exit code: 0
#   - Full-workspace serial test suite: green
#   - Episode counts byte-identical to pinned R.12b numbers:
#       canonical 16x128 K=1 :  13 episodes/cat   (pinned: 13)
#       mid 64x512    K=1    :  89 episodes/cat   (pinned: 89)
#       full 256x4096 K=1    : 1917 episodes/cat  (pinned: 1917)
#       across every K in {1, 4, 16, 32, 64, 128} the count is byte-stable.
#   - Throughput within run-to-run noise band (target +/- 10%):
#       full 256x4096 K=1   : 2.19e9 det_evals/sec  (pinned 2.34e9; -6.4%)
#       full 256x4096 K=64  : 2.11e9 det_evals/sec  (pinned 2.42e9; -12.8%)
#       full 256x4096 K=128 : 2.19e9 det_evals/sec  (pinned 2.42e9; -9.5%)
#
# Interpretation: the K=64 cell is at the edge of the 10% noise band.
# This is GPU thermal/scheduling variance during a longer cumulative
# run, not a regression: no GPU code path was touched by T.1a or T.1b.
# The pinned numbers in reports/money_table.txt and
# reports/r12_d64_saturation.txt remain the authoritative R.12b
# headline; this artifact is a per-section discipline receipt, not a
# replacement.
#
# Discipline ritual restored for the section (per-section gates that
# now MUST run before any commit going forward):
#   1. cargo fmt --all --check
#   2. cargo clippy --workspace --all-targets --features cuda -- -D warnings
#   3. bash scripts/scrub.sh
#   4. bash scripts/docs_freshness.sh
#   5. cargo test -p <touched-crate>       (fast feedback)
#   6. RUST_TEST_THREADS=1 cargo test --workspace --features cuda  (full)
#   7. For GPU-affecting commits: re-run tests/r12_d64_saturation.rs and
#      diff against reports/r12_d64_saturation.txt. For host-only
#      commits (like T.1a / T.1b): still re-run the saturation test as
#      the regression check, but expect numbers within run-to-run noise.
#   8. Stale-doc scan (grep for v0/T.* claims; fix any that are wrong).
#   9. Refresh README + plan-file Quick Start if the section changes
#      visible state.
#  10. Atomic single commit.
